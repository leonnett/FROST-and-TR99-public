# TR99 evidence

This folder separates a fresh audit run from an earlier result documented by
the source repository.

- [`verification-summary.json`](verification-summary.json) contains both
  provenance-qualified records.

The exact-tokenizer record is preserved as source-documented evidence because
the original raw JSON artifact is not committed in the source branch. It is
not presented as a fresh rerun by this collection.
